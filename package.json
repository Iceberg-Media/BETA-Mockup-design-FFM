{
  "name": "fansfollow-preview",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "serve -l 3000",
    "build": "mkdir -p dist/assets/css && cp *.html dist/ && cp -r assets/css/*.css dist/assets/css/"
  },
  "devDependencies": {
    "serve": "^14.2.4"
  }
}
