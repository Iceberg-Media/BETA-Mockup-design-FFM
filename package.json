{
  "name": "fansfollow-preview",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "serve -l 3000",
    "build": "mkdir -p dist/assets/css dist/assets/img && cp *.html dist/ && cp -r assets/css/*.css dist/assets/css/ && cp assets/img/* dist/assets/img/"
  },
  "devDependencies": {
    "serve": "^14.2.4"
  }
}
